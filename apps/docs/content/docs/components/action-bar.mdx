---
title: Action Bar
description: Fixed action bar for confirming deletions.
---

<ComponentPreview name="action-bar-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<CodeBlockCommand command="npx shadcn@latest add https://matos-ui.com/r/action-bar.json" />

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

<CodeBlockCommand command="npm install lucide-react tailwind-merge tailwind-variants" />

<Step>Install the registry dependency:</Step>

<CodeBlockCommand command="npx shadcn@latest add https://matos-ui.com/r/button.json" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="action-bar" title="components/matos-ui/action-bar.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ActionBar } from '@/components/matos-ui/action-bar'
```

```tsx
<ActionBar
  subject="My Workspace"
  tone="destructive"
  placement="bottomCenter"
  actions={{
    onConfirm: () => {},
    onCancel: () => {},
    isLoading: false
  }}
/>
```

## Reference

### Props

| Prop        | Type                                                                    | Default          |
| ----------- | ----------------------------------------------------------------------- | ---------------- |
| `subject`   | `string`                                                                | —                |
| `placement` | `"bottomCenter" \| "bottomRight" \| "bottomLeft"`                       | `"bottomCenter"` |
| `tone`      | `"default" \| "destructive"`                                            | `"default"`      |
| `size`      | `"sm" \| "md"`                                                          | `"md"`           |
| `actions`   | `{ onConfirm: () => void; onCancel: () => void; isDeleting?: boolean }` | —                |

`ActionBar` also accepts all props from `<div>`.
